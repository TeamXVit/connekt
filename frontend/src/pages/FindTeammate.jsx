import { Container, Typography } from "@mui/material";


export default function FindTeammate() {
    return (
        <Container maxWidth={false} sx={{  bgcolor: "background.default", color: "text.primary", height: "100vh", display: "flex", justifyContent: "center", alignItems: "center", }}>
            <Typography variant="h3">Coming soon.....</Typography>
        </Container>
    );
};